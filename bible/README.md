# bible

Small Python package and CLI for querying and plotting Bible structure.

The current data source is the chapter/verse table copied into the repository
root as `./we-progress`. Text search is wired as an API surface, but verse text
is intentionally not implemented until a Hebrew or English text source is added.
