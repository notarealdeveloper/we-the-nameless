#!/usr/bin/env bash

lord --verbose << "EOF"
I PREVIOUSLY ASKED FOR THIS:

Add a config option to master.tex called IndividualBookTitlePageStyle with values 'plain' and 'fancy'. The 'plain' option should give the current behavior, while 'fancy' should have the following:

Fancy should arise from the following: 1. Add TOC.md's data to master.tex in a way that makes it easy for you to include that data in each individual book's title page, thus making each book's title page (e.g., Genesis, Exodus, etc) contain the summaries in TOC.md in place of the stuff where the numerical hyperlinks currently are. Make the 2-3 word descriptions from TOC.md along with Book: Chapter names (e.g., Genesis 1: Creation) into hyperlinks. That's \\IndividualBookTitlePageStyle when set to fancy. \\IndividualBookTitlePageStyle when set to plain should be the current behavior.

THEN I ASKED FOR THIS:

You added the config option but it doesn't work correctly.

Let's explain more simply.

1. Read ./TOC.md in the repo root.
2. Take exactly that content, and make it into a per-book table of contents along the lines of './TOC TeX.md' (reimplement it using the primitives in master.tex or by adding your own though, don't be married to ./TOC TeX.md, it's just a rough outline of the general idea.)
3. When \\IndividualBookTitlePageStyle is set to fancy, I want Genesis, Exodus, Leviticus, etc. to each have per-book tables of contents on their Book title pages where the links currently are. The top level TOC should stay the same: i.e., Genesis starts on page 1, Exodus on page etc. Each as a hyperlink. However, the *Genesis* (resp. Exodus and the rest) book title pages should no longer have the GRID of links in fancy mode. Instead they should have something like:

## Genesis

- Genesis 1 — Creation.
- Genesis 2 — Adam and Eve.
- Genesis 3 — Serpent; exile.
- Genesis 4 — Cain and Abel.
- Genesis 5 — Adam's line.
- Genesis 6 — Flood coming.

All as hyperlinks to the chapters in question.

Get it? Great. Do that.


THEN I ASKED FOR THIS:

Great! Now replace things like:

Genesis 1 — Creation

with

1. Creation

in the book-specific ToC hyperlinks.

THEN I ASKED FOR THIS:

I made some very minor changes to TOC.md. Merge the updates into the tex TOC described above. Keep the '1. Creation' format etc, but update with my changes, e.g., see Genesis 9 summary for one example of several.

I NOW ASK FOR THIS:

Your implementation reads from TOC.md. Don't do that. Turn it into tex and merge it into master.tex so I can delete TOC.md afterwards.
EOF
