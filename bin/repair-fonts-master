#!/usr/bin/env python3
"""Repair and normalize the WTN ancient alphabet master fonts."""

from __future__ import annotations

import importlib.util
import math
import shutil
from importlib.machinery import SourceFileLoader
from pathlib import Path

import fontforge
import psMat
from PIL import Image


ROOT = Path(__file__).resolve().parents[1]
FONT_DIR = ROOT / "fonts-master"
README = FONT_DIR / "README.md"
TABLE_KEYS = ["a", "b", "g", "d", "e", "w", "z", "h", "T", "y", "k", "l", "m", "n", "S", "o", "p", "c", "q", "r", "s", "t"]
MIN_AREA = 10_000
REPAIR_VERSION = "1.6"

RENAMES = {
    "03-bc15c-proto-sinaitic-variant-a.ttf": "03-bc15c-proto-sinaitic-generic-a.ttf",
    "03-bc15c-proto-sinaitic-variant-b.ttf": "03-bc15c-proto-sinaitic-generic-b.ttf",
    "03-bc15c-proto-sinaitic-wadi-el-hol-inscription.ttf": "02-bc19c-proto-sinaitic-wadi-el-hol-inscription.ttf",
    "03-bc15c-proto-sinaitic-serabit-el-khadim-inscription.ttf": "03-bc18c-proto-sinaitic-serabit-el-khadim-inscription.ttf",
    "03-bc15c-proto-sinaitic-15th-century-a.ttf": "04-bc15c-proto-sinaitic-15th-century-a.ttf",
    "03-bc15c-proto-sinaitic-15th-century-b.ttf": "04-bc15c-proto-sinaitic-15th-century-b.ttf",
    "03-bc15c-proto-sinaitic-15th-century-light.ttf": "04-bc15c-proto-sinaitic-15th-century-light.ttf",
    "03-bc15c-proto-sinaitic-15th-century.ttf": "04-bc15c-proto-sinaitic-15th-century-c.ttf",
    "03-bc15c-proto-sinaitic-generic-a.ttf": "04-bc15c-proto-sinaitic-generic-a.ttf",
    "03-bc15c-proto-sinaitic-generic-b.ttf": "04-bc15c-proto-sinaitic-generic-b.ttf",
    "04-bc15c-proto-sinaitic-15th-century.ttf": "04-bc15c-proto-sinaitic-15th-century-c.ttf",
    "04-bc15c-proto-sinaitic-generic-a.ttf": "04-bc15c-proto-sinaitic-15th-century-d.ttf",
    "04-bc15c-proto-sinaitic-generic-b.ttf": "04-bc15c-proto-sinaitic-15th-century-e.ttf",
    "04-bc14c-ugaritic-noto-sans.ttf": "05-bc14c-ugaritic-noto-sans.ttf",
    "05-bc13c-proto-canaanite-a.ttf": "06-bc13c-proto-canaanite-a.ttf",
    "05-bc13c-proto-canaanite-b.ttf": "06-bc13c-proto-canaanite-b.ttf",
    "06-bc12c-proto-canaanite-izbet-sartah-a.ttf": "07-bc12c-proto-canaanite-izbet-sartah-a.ttf",
    "06-bc12c-proto-canaanite-izbet-sartah-b.ttf": "07-bc12c-proto-canaanite-izbet-sartah-b.ttf",
    "07-bc10c-paleo-hebrew-gezer-a.ttf": "08-bc10c-paleo-hebrew-gezer-a.ttf",
    "07-bc10c-paleo-hebrew-gezer-b.ttf": "08-bc10c-paleo-hebrew-gezer-b.ttf",
    "07-bc10c-paleo-hebrew-tel-zayit.ttf": "08-bc10c-paleo-hebrew-tel-zayit.ttf",
    "07-bc10c-phoenician-noto-sans.ttf": "08-bc10c-phoenician-noto-sans.ttf",
    "08-bc10c-phoenician-noto-sans.ttf": "08-bc10c-paleo-hebrew-noto-sans.ttf",
    "08-bc09c-paleo-hebrew-mesha-stele-a.ttf": "09-bc09c-paleo-hebrew-mesha-stele-a.ttf",
    "08-bc09c-paleo-hebrew-mesha-stele-b.ttf": "09-bc09c-paleo-hebrew-mesha-stele-b.ttf",
    "08-bc09c-paleo-hebrew-mono.ttf": "09-bc09c-paleo-hebrew-mono.ttf",
    "08-bc09c-paleo-hebrew-robo.ttf": "09-bc09c-paleo-hebrew-robo.ttf",
    "08-bc09c-paleo-hebrew-standard-a.ttf": "09-bc09c-paleo-hebrew-standard-a.ttf",
    "08-bc09c-paleo-hebrew-standard-b.ttf": "09-bc09c-paleo-hebrew-standard-b.ttf",
    "08-bc09c-paleo-hebrew-tel-dan-a.ttf": "09-bc09c-paleo-hebrew-tel-dan-a.ttf",
    "08-bc09c-paleo-hebrew-tel-dan-b.ttf": "09-bc09c-paleo-hebrew-tel-dan-b.ttf",
    "08-bc09c-paleo-hebrew-yoram-gnat.ttf": "09-bc09c-paleo-hebrew-yoram-gnat.ttf",
    "09-bc08c-paleo-hebrew-siloam-a.ttf": "10-bc08c-paleo-hebrew-siloam-a.ttf",
    "09-bc08c-paleo-hebrew-siloam-b.ttf": "10-bc08c-paleo-hebrew-siloam-b.ttf",
    "10-bc07c-paleo-hebrew-ketef-hinnom-a.ttf": "11-bc07c-paleo-hebrew-ketef-hinnom-a.ttf",
    "10-bc07c-paleo-hebrew-ketef-hinnom-b.ttf": "11-bc07c-paleo-hebrew-ketef-hinnom-b.ttf",
    "10-bc07c-paleo-hebrew-yavneh-yam-a.ttf": "11-bc07c-paleo-hebrew-yavneh-yam-a.ttf",
    "10-bc07c-paleo-hebrew-yavneh-yam-b.ttf": "11-bc07c-paleo-hebrew-yavneh-yam-b.ttf",
    "11-bc06c-paleo-hebrew-lachish-3.ttf": "12-bc06c-paleo-hebrew-lachish-3.ttf",
    "11-bc06c-paleo-hebrew-lachish-4.ttf": "12-bc06c-paleo-hebrew-lachish-4.ttf",
    "11-bc06c-paleo-hebrew-lachish-5.ttf": "12-bc06c-paleo-hebrew-lachish-5.ttf",
    "12-bc05c-imperial-aramaic-noto-sans.ttf": "13-bc05c-imperial-aramaic-noto-sans.ttf",
    "13-bc02c-paleo-hebrew-isaiah-scroll-a.ttf": "14-bc02c-paleo-hebrew-isaiah-scroll-a.ttf",
    "13-bc02c-paleo-hebrew-isaiah-scroll-b.ttf": "14-bc02c-paleo-hebrew-isaiah-scroll-b.ttf",
    "14-bc02c-paleo-hebrew-isaiah-scroll-a.ttf": "14-bc02c-dead-sea-scrolls-isaiah-a.ttf",
    "14-bc02c-paleo-hebrew-isaiah-scroll-b.ttf": "14-bc02c-dead-sea-scrolls-isaiah-b.ttf",
}

METADATA = {
    "01-bc32c-egyptian-hieroglyphs-noto-sans.ttf": ("WTN Egyptian Hieroglyphs Noto Sans", "Regular"),
    "02-bc19c-proto-sinaitic-wadi-el-hol-inscription.ttf": (
        "WTN Proto Sinaitic Wadi el-Hol 19th Century BCE",
        "Bold",
    ),
    "03-bc18c-proto-sinaitic-serabit-el-khadim-inscription.ttf": (
        "WTN Proto Sinaitic Serabit el-Khadim 18th Century BCE",
        "Bold",
    ),
    "03-bc18c-proto-sinaitic-serabit-el-khadim-inscription-light.ttf": (
        "WTN Proto Sinaitic Serabit el-Khadim 18th Century BCE",
        "Light",
    ),
    "08-bc10c-paleo-hebrew-noto-sans.ttf": ("WTN Paleo Hebrew Noto Sans", "Regular"),
    "14-bc02c-dead-sea-scrolls-isaiah-a.ttf": ("WTN Dead Sea Scrolls Isaiah A", "Regular"),
    "14-bc02c-dead-sea-scrolls-isaiah-b.ttf": ("WTN Dead Sea Scrolls Isaiah B", "Regular"),
    "04-bc15c-proto-sinaitic-15th-century-a.ttf": ("WTN Proto Sinaitic 15th Century A", "Regular"),
    "04-bc15c-proto-sinaitic-15th-century-b.ttf": ("WTN Proto Sinaitic 15th Century B", "Regular"),
    "04-bc15c-proto-sinaitic-15th-century-c.ttf": ("WTN Proto Sinaitic 15th Century C", "Regular"),
    "04-bc15c-proto-sinaitic-15th-century-d.ttf": ("WTN Proto Sinaitic 15th Century D", "Regular"),
    "04-bc15c-proto-sinaitic-15th-century-e.ttf": ("WTN Proto Sinaitic 15th Century E", "Regular"),
}

SUBSTITUTION_OVERRIDES = {
    ("03-bc18c-proto-sinaitic-serabit-el-khadim-inscription.ttf", "e"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "e",
    ),
    ("03-bc18c-proto-sinaitic-serabit-el-khadim-inscription.ttf", "S"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "S",
    ),
    ("03-bc18c-proto-sinaitic-serabit-el-khadim-inscription.ttf", "s"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "s",
    ),
    ("02-bc19c-proto-sinaitic-wadi-el-hol-inscription.ttf", "d"): (
        "03-bc18c-proto-sinaitic-serabit-el-khadim-inscription.ttf",
        "d",
    ),
    ("02-bc19c-proto-sinaitic-wadi-el-hol-inscription.ttf", "K"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "k",
    ),
    ("02-bc19c-proto-sinaitic-wadi-el-hol-inscription.ttf", "k"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "k",
    ),
    ("02-bc19c-proto-sinaitic-wadi-el-hol-inscription.ttf", "l"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "l",
    ),
    ("02-bc19c-proto-sinaitic-wadi-el-hol-inscription.ttf", "S"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "S",
    ),
    ("02-bc19c-proto-sinaitic-wadi-el-hol-inscription.ttf", "C"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "c",
    ),
    ("02-bc19c-proto-sinaitic-wadi-el-hol-inscription.ttf", "c"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "c",
    ),
}

GLYPH_REPLACEMENTS = {
    ("02-bc19c-proto-sinaitic-wadi-el-hol-inscription.ttf", "T"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "T",
    ),
    ("02-bc19c-proto-sinaitic-wadi-el-hol-inscription.ttf", "z"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "z",
    ),
    ("02-bc19c-proto-sinaitic-wadi-el-hol-inscription.ttf", "o"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "o",
    ),
    ("02-bc19c-proto-sinaitic-wadi-el-hol-inscription.ttf", "q"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "q",
    ),
    ("02-bc19c-proto-sinaitic-wadi-el-hol-inscription.ttf", "k"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "k",
    ),
    ("02-bc19c-proto-sinaitic-wadi-el-hol-inscription.ttf", "l"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "l",
    ),
    ("02-bc19c-proto-sinaitic-wadi-el-hol-inscription.ttf", "S"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "S",
    ),
    ("02-bc19c-proto-sinaitic-wadi-el-hol-inscription.ttf", "c"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "c",
    ),
    ("02-bc19c-proto-sinaitic-wadi-el-hol-inscription.ttf", "n"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "n",
    ),
    ("04-bc15c-proto-sinaitic-15th-century-a.ttf", "o"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "o",
    ),
    ("04-bc15c-proto-sinaitic-15th-century-b.ttf", "o"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "o",
    ),
    ("03-bc18c-proto-sinaitic-serabit-el-khadim-inscription.ttf", "T"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "T",
    ),
    ("03-bc18c-proto-sinaitic-serabit-el-khadim-inscription.ttf", "z"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "z",
    ),
    ("03-bc18c-proto-sinaitic-serabit-el-khadim-inscription.ttf", "o"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "o",
    ),
    ("03-bc18c-proto-sinaitic-serabit-el-khadim-inscription.ttf", "q"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "q",
    ),
    ("03-bc18c-proto-sinaitic-serabit-el-khadim-inscription.ttf", "s"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "s",
    ),
    ("04-bc15c-proto-sinaitic-15th-century-a.ttf", "S"): (
        "08-bc10c-paleo-hebrew-gezer-a.ttf",
        "S",
    ),
    ("04-bc15c-proto-sinaitic-15th-century-b.ttf", "S"): (
        "08-bc10c-paleo-hebrew-gezer-b.ttf",
        "S",
    ),
    ("04-bc15c-proto-sinaitic-15th-century-d.ttf", "T"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "T",
    ),
    ("04-bc15c-proto-sinaitic-15th-century-d.ttf", "z"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "z",
    ),
    ("04-bc15c-proto-sinaitic-15th-century-d.ttf", "o"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "o",
    ),
    ("04-bc15c-proto-sinaitic-15th-century-d.ttf", "q"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "q",
    ),
    ("04-bc15c-proto-sinaitic-15th-century-e.ttf", "T"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "T",
    ),
    ("04-bc15c-proto-sinaitic-15th-century-e.ttf", "z"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "z",
    ),
    ("04-bc15c-proto-sinaitic-15th-century-e.ttf", "o"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "o",
    ),
    ("04-bc15c-proto-sinaitic-15th-century-e.ttf", "q"): (
        "04-bc15c-proto-sinaitic-15th-century-c.ttf",
        "q",
    ),
    ("04-bc15c-proto-sinaitic-15th-century-e.ttf", "S"): (
        "08-bc10c-paleo-hebrew-gezer-b.ttf",
        "S",
    ),
    ("08-bc10c-paleo-hebrew-gezer-a.ttf", "g"): (
        "08-bc10c-paleo-hebrew-tel-zayit.ttf",
        "g",
    ),
    ("08-bc10c-paleo-hebrew-gezer-a.ttf", "e"): (
        "08-bc10c-paleo-hebrew-tel-zayit.ttf",
        "e",
    ),
    ("08-bc10c-paleo-hebrew-gezer-a.ttf", "T"): (
        "08-bc10c-paleo-hebrew-tel-zayit.ttf",
        "T",
    ),
    ("08-bc10c-paleo-hebrew-gezer-a.ttf", "n"): (
        "08-bc10c-paleo-hebrew-tel-zayit.ttf",
        "n",
    ),
    ("08-bc10c-paleo-hebrew-gezer-b.ttf", "g"): (
        "08-bc10c-paleo-hebrew-tel-zayit.ttf",
        "g",
    ),
    ("08-bc10c-paleo-hebrew-gezer-b.ttf", "e"): (
        "08-bc10c-paleo-hebrew-tel-zayit.ttf",
        "e",
    ),
    ("08-bc10c-paleo-hebrew-gezer-b.ttf", "T"): (
        "08-bc10c-paleo-hebrew-tel-zayit.ttf",
        "T",
    ),
    ("08-bc10c-paleo-hebrew-gezer-b.ttf", "n"): (
        "08-bc10c-paleo-hebrew-tel-zayit.ttf",
        "n",
    ),
    ("09-bc09c-paleo-hebrew-mesha-stele-a.ttf", "o"): (
        "09-bc09c-paleo-hebrew-tel-dan-a.ttf",
        "o",
    ),
    ("09-bc09c-paleo-hebrew-mesha-stele-b.ttf", "o"): (
        "09-bc09c-paleo-hebrew-tel-dan-a.ttf",
        "o",
    ),
    ("09-bc09c-paleo-hebrew-standard-a.ttf", "o"): (
        "09-bc09c-paleo-hebrew-tel-dan-a.ttf",
        "o",
    ),
    ("09-bc09c-paleo-hebrew-standard-b.ttf", "o"): (
        "09-bc09c-paleo-hebrew-tel-dan-a.ttf",
        "o",
    ),
}

SVG_REPLACEMENTS = {
    ("02-bc19c-proto-sinaitic-wadi-el-hol-inscription.ttf", "o"): (
        "fonts-proto-sinaitic-wadi-el-hol/Wadi_el-hol-O.svg",
        1.0,
    ),
    ("06-bc13c-proto-canaanite-a.ttf", "o"): (
        "fonts-proto-sinaitic-generic/Proto-semiticO-01.svg",
        0.78,
    ),
    ("06-bc13c-proto-canaanite-b.ttf", "o"): (
        "fonts-proto-sinaitic-generic/Proto-semiticO-01.svg",
        0.78,
    ),
    ("07-bc12c-proto-canaanite-izbet-sartah-a.ttf", "o"): (
        "fonts-proto-sinaitic-generic/Proto-semiticO-01.svg",
        0.78,
    ),
    ("07-bc12c-proto-canaanite-izbet-sartah-b.ttf", "o"): (
        "fonts-proto-sinaitic-generic/Proto-semiticO-01.svg",
        0.78,
    ),
    ("08-bc10c-paleo-hebrew-gezer-a.ttf", "o"): (
        "fonts-proto-sinaitic-generic/Proto-semiticO-01.svg",
        0.72,
    ),
    ("08-bc10c-paleo-hebrew-gezer-b.ttf", "o"): (
        "fonts-proto-sinaitic-generic/Proto-semiticO-01.svg",
        0.72,
    ),
}

BITMAP_REPLACEMENTS = {
    ("02-bc19c-proto-sinaitic-wadi-el-hol-inscription.ttf", "h"): (
        "fonts-proto-sinaitic-serabit/Serabit-El-Khadim-Ḥ362.svg.webp",
        0.84,
    ),
}

BOLDEN_FONTS = {
    "02-bc19c-proto-sinaitic-wadi-el-hol-inscription.ttf",
    "03-bc18c-proto-sinaitic-serabit-el-khadim-inscription.ttf",
}
OBSOLETE_FONTS = {
    "04-bc15c-proto-sinaitic-15th-century-light.ttf",
}
WADI_FONT = "02-bc19c-proto-sinaitic-wadi-el-hol-inscription.ttf"
SERABIT_FONT = "03-bc18c-proto-sinaitic-serabit-el-khadim-inscription.ttf"
EGYPTIAN_FONT = "01-bc32c-egyptian-hieroglyphs-noto-sans.ttf"
EGYPTIAN_SOURCE = ROOT / "fonts" / "egyptian-hieroglyphs-regular-noto-sans.ttf"
EGYPTIAN_TAIL = {
    "c": 0x131D1,
    "q": 0x13397,
    "r": 0x13076,
    "s": 0x13314,
    "t": 0x133F4,
}

FINAL_FORM_FALLBACKS = {
    "K": "k",
    "M": "m",
    "N": "n",
    "P": "p",
    "C": "c",
}


def load_alphabet():
    path = ROOT / "bin" / "alphabet"
    spec = importlib.util.spec_from_loader("wtn_alphabet", SourceFileLoader("wtn_alphabet", str(path)))
    if spec is None or spec.loader is None:
        raise RuntimeError(f"could not load {path}")
    module = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(module)
    return module


def font_files() -> list[Path]:
    return sorted(FONT_DIR.glob("*.ttf"))


def glyph_area(glyph) -> float:
    bbox = glyph.boundingBox()
    return max(0.0, bbox[2] - bbox[0]) * max(0.0, bbox[3] - bbox[1])


def has_real_glyph(font, char: str) -> bool:
    codepoint = ord(char)
    if codepoint not in font:
        return False
    glyph = font[codepoint]
    return glyph.glyphname != ".notdef" and glyph.isWorthOutputting() and glyph_area(glyph) >= MIN_AREA


def copy_glyph(source, source_char: str, target, target_char: str) -> None:
    target.createChar(ord(target_char), target_char)
    source.selection.none()
    target.selection.none()
    source.selection.select(ord(source_char))
    source.copy()
    target.selection.select(ord(target_char))
    target.paste()
    glyph = target[ord(target_char)]
    glyph.glyphname = target_char
    glyph.unicode = ord(target_char)


def copy_source_codepoint(source, source_codepoint: int, target, target_char: str, scale: float = 0.846) -> None:
    target.createChar(ord(target_char), target_char)
    source.selection.none()
    target.selection.none()
    source.selection.select(source_codepoint)
    source.copy()
    target.selection.select(ord(target_char))
    target.paste()
    glyph = target[ord(target_char)]
    glyph.glyphname = target_char
    glyph.unicode = ord(target_char)
    glyph.transform(psMat.scale(scale))
    glyph.round()
    xmin, _ymin, xmax, _ymax = glyph.boundingBox()
    glyph.transform(psMat.translate(60 - xmin, 0))
    glyph.width = int(round((xmax - xmin) + 120))


def fit_glyph(font, char: str, max_width: float = 720, max_height: float = 560) -> None:
    glyph = font[ord(char)]
    xmin, ymin, xmax, ymax = glyph.boundingBox()
    width = xmax - xmin
    height = ymax - ymin
    if width <= 0 or height <= 0:
        return
    scale = min(max_width / width, max_height / height, 1.0)
    glyph.transform(psMat.translate(-xmin, -ymin))
    glyph.transform(psMat.scale(scale))
    glyph.round()
    xmin, ymin, xmax, _ymax = glyph.boundingBox()
    glyph.transform(psMat.translate(60 - xmin, 90 - ymin))
    glyph.width = int(round((xmax - xmin) + 120))


def import_svg_to_key(font, char: str, svg_path: str, scale_factor: float = 1.0) -> None:
    glyph = font.createChar(ord(char), char)
    glyph.clear()
    glyph.glyphname = char
    glyph.unicode = ord(char)
    glyph.importOutlines(str(ROOT / svg_path))
    glyph.removeOverlap()
    glyph.correctDirection()
    xmin, ymin, xmax, ymax = glyph.boundingBox()
    width = xmax - xmin
    height = ymax - ymin
    if width <= 0 or height <= 0:
        return
    scale = min(584 / height, 900 / width) * scale_factor
    glyph.transform(psMat.scale(scale))
    glyph.round()
    xmin, ymin, xmax, _ymax = glyph.boundingBox()
    glyph.transform(psMat.translate(60 - xmin, 90 - ymin))
    glyph.width = int(round((xmax - xmin) + 120))


def bitmap_boundary_loops(mask: list[list[bool]]) -> list[list[tuple[int, int]]]:
    height = len(mask)
    width = len(mask[0]) if height else 0
    edges: dict[tuple[int, int], list[tuple[int, int]]] = {}

    def filled(x: int, y: int) -> bool:
        return 0 <= x < width and 0 <= y < height and mask[y][x]

    def add_edge(start: tuple[int, int], end: tuple[int, int]) -> None:
        edges.setdefault(start, []).append(end)

    for y in range(height):
        for x in range(width):
            if not mask[y][x]:
                continue
            if not filled(x, y - 1):
                add_edge((x, y), (x + 1, y))
            if not filled(x + 1, y):
                add_edge((x + 1, y), (x + 1, y + 1))
            if not filled(x, y + 1):
                add_edge((x + 1, y + 1), (x, y + 1))
            if not filled(x - 1, y):
                add_edge((x, y + 1), (x, y))

    loops = []
    while edges:
        start = next(iter(edges))
        current = start
        loop = [start]
        while True:
            next_points = edges.get(current)
            if not next_points:
                break
            next_point = next_points.pop()
            if not next_points:
                del edges[current]
            current = next_point
            if current == start:
                break
            loop.append(current)
        if len(loop) >= 3 and current == start:
            loops.append(simplify_orthogonal_loop(loop))
    return loops


def simplify_orthogonal_loop(loop: list[tuple[int, int]]) -> list[tuple[int, int]]:
    changed = True
    simplified = loop
    while changed and len(simplified) > 3:
        changed = False
        result = []
        count = len(simplified)
        for index, point in enumerate(simplified):
            prev_point = simplified[(index - 1) % count]
            next_point = simplified[(index + 1) % count]
            if (
                (prev_point[0] == point[0] == next_point[0])
                or (prev_point[1] == point[1] == next_point[1])
            ):
                changed = True
                continue
            result.append(point)
        simplified = result
    return simplified


def import_bitmap_alpha_to_key(font, char: str, image_path: str, scale_factor: float = 1.0) -> None:
    image = Image.open(ROOT / image_path).convert("RGBA")
    alpha = image.getchannel("A")
    bbox = alpha.getbbox()
    if bbox is None:
        return

    alpha = alpha.crop(bbox)
    target_height = 220
    scale = target_height / alpha.height
    target_width = max(1, int(round(alpha.width * scale)))
    alpha = alpha.resize((target_width, target_height), Image.Resampling.LANCZOS)
    threshold = 72
    mask = [[alpha.getpixel((x, y)) > threshold for x in range(alpha.width)] for y in range(alpha.height)]
    loops = bitmap_boundary_loops(mask)
    if not loops:
        return

    glyph = font.createChar(ord(char), char)
    glyph.clear()
    glyph.glyphname = char
    glyph.unicode = ord(char)
    pen = glyph.glyphPen()
    height = alpha.height
    for loop in loops:
        first_x, first_y = loop[0]
        pen.moveTo((first_x, height - first_y))
        for x, y in loop[1:]:
            pen.lineTo((x, height - y))
        pen.closePath()

    glyph.removeOverlap()
    glyph.correctDirection()
    xmin, ymin, xmax, ymax = glyph.boundingBox()
    width = xmax - xmin
    glyph_height = ymax - ymin
    if width <= 0 or glyph_height <= 0:
        return
    scale = min(584 / glyph_height, 900 / width) * scale_factor
    glyph.transform(psMat.scale(scale))
    glyph.round()
    xmin, ymin, xmax, _ymax = glyph.boundingBox()
    glyph.transform(psMat.translate(60 - xmin, 90 - ymin))
    glyph.width = int(round((xmax - xmin) + 120))


def rename_files() -> None:
    for old, new in RENAMES.items():
        old_path = FONT_DIR / old
        new_path = FONT_DIR / new
        if old_path.exists() and not new_path.exists():
            old_path.rename(new_path)
    for filename in OBSOLETE_FONTS:
        path = FONT_DIR / filename
        if path.exists():
            path.unlink()


def preserve_serabit_light() -> None:
    regular = FONT_DIR / "03-bc18c-proto-sinaitic-serabit-el-khadim-inscription.ttf"
    light = FONT_DIR / "03-bc18c-proto-sinaitic-serabit-el-khadim-inscription-light.ttf"
    if regular.exists() and not light.exists():
        shutil.copy2(regular, light)


def update_metadata(font, filename: str) -> None:
    family, style = METADATA.get(filename, (font.familyname, font.weight or "Regular"))
    font.familyname = family
    font.fontname = family.replace(" ", "-")
    font.fullname = f"{family} {style}"
    font.weight = style
    font.copyright = "Prepared for We The Nameless from source fonts and inscriptional SVG reconstructions."
    font.version = REPAIR_VERSION
    font.sfnt_names = tuple(
        row for row in font.sfnt_names if row[1] not in {"Family", "SubFamily", "Fullname", "UniqueID", "Version"}
    ) + (
        ("English (US)", "Family", family),
        ("English (US)", "SubFamily", style),
        ("English (US)", "Fullname", f"{family} {style}"),
        ("English (US)", "UniqueID", f"{family} {style} {REPAIR_VERSION}"),
        ("English (US)", "Version", f"Version {REPAIR_VERSION}"),
    )


def nearest_valid_source(fonts: dict[str, object], filenames: list[str], filename: str, char: str) -> tuple[str, str]:
    index = filenames.index(filename)
    candidates = []
    for source_index, source_name in enumerate(filenames):
        if source_name == filename:
            continue
        if has_real_glyph(fonts[source_name], char):
            candidates.append((abs(source_index - index), source_index, source_name))
    if not candidates:
        raise RuntimeError(f"no valid donor found for {filename}:{char}")
    _, _, source_name = min(candidates)
    return source_name, char


def embolden(font, amount: int = 34) -> None:
    for glyph in font.glyphs():
        if glyph.unicode < 0 or glyph.glyphname == ".notdef" or not glyph.isWorthOutputting() or glyph_area(glyph) < MIN_AREA:
            continue
        glyph.stroke("circular", amount)
        glyph.removeOverlap()
        glyph.simplify()


def embolden_char(font, char: str, amount: int) -> None:
    codepoint = ord(char)
    if codepoint not in font:
        return
    glyph = font[codepoint]
    if glyph.glyphname == ".notdef" or not glyph.isWorthOutputting() or glyph_area(glyph) < MIN_AREA:
        return
    glyph.stroke("circular", amount)
    glyph.removeOverlap()
    glyph.simplify()


def contour_bbox(contour) -> tuple[float, float, float, float]:
    xs = [point.x for point in contour]
    ys = [point.y for point in contour]
    return min(xs), min(ys), max(xs), max(ys)


def bbox_contains(outer: tuple[float, float, float, float], inner: tuple[float, float, float, float]) -> bool:
    ox1, oy1, ox2, oy2 = outer
    ix1, iy1, ix2, iy2 = inner
    return ox1 <= ix1 and oy1 <= iy1 and ox2 >= ix2 and oy2 >= iy2


def bbox_area(bbox: tuple[float, float, float, float]) -> float:
    x1, y1, x2, y2 = bbox
    return max(0.0, x2 - x1) * max(0.0, y2 - y1)


def fill_hollow_components(font) -> None:
    for glyph in font.glyphs():
        if glyph.unicode < 0 or glyph.glyphname == ".notdef" or not glyph.isWorthOutputting():
            continue
        layer = glyph.foreground.dup()
        bboxes = [contour_bbox(contour) for contour in layer]
        drop = set()
        for index, bbox in enumerate(bboxes):
            for other_index, other_bbox in enumerate(bboxes):
                if index == other_index:
                    continue
                if bbox_area(other_bbox) > bbox_area(bbox) and bbox_contains(other_bbox, bbox):
                    drop.add(index)
                    break
        for index in sorted(drop, reverse=True):
            del layer[index]
        glyph.foreground = layer
        glyph.round()


def repair_egyptian_tail(font) -> None:
    source = fontforge.open(str(EGYPTIAN_SOURCE))
    for target_char, source_codepoint in EGYPTIAN_TAIL.items():
        copy_source_codepoint(source, source_codepoint, font, target_char)
    source.close()


def replace_with_filled_oval(font, char: str) -> None:
    codepoint = ord(char)
    if codepoint not in font:
        return

    glyph = font[codepoint]
    xmin, ymin, xmax, ymax = glyph.boundingBox()
    width = max(260.0, xmax - xmin)
    height = max(150.0, ymax - ymin, width * 0.42)
    cx = (xmin + xmax) / 2
    cy = (ymin + ymax) / 2
    rx = width / 2
    ry = height / 2

    glyph.clear()
    glyph.width = int(round(width + 120))
    glyph.glyphname = char
    glyph.unicode = codepoint
    pen = glyph.glyphPen()
    steps = 32
    for step in range(steps):
        angle = (2 * math.pi * step) / steps
        point = (cx + (rx * math.cos(angle)), cy + (ry * math.sin(angle)))
        if step == 0:
            pen.moveTo(point)
        else:
            pen.lineTo(point)
    pen.closePath()
    xmin, _ymin, xmax, _ymax = glyph.boundingBox()
    glyph.transform(psMat.translate(60 - xmin, 0))
    glyph.width = int(round((xmax - xmin) + 120))


def prune_to_key_layer(font, allowed: set[int]) -> None:
    for glyph in list(font.glyphs()):
        if glyph.unicode < 0 or glyph.unicode in allowed:
            continue
        font.selection.none()
        font.selection.select(glyph.unicode)
        font.clear()


def write_readme(
    substitutions: list[tuple[str, str, str, str]],
    replacements: list[tuple[str, str, str, str]],
) -> None:
    lines = [
        "# WTN Master Font Repairs",
        "",
        "These fonts encode one semantic value space: the Hebrew alphabet rendered in several historical hands.",
        "The canonical key map is `../bin/alphabet`; every Hebrew, Latin, Greek, and Cyrillic alias in that map is copied from the corresponding repaired master glyph inside each font.",
        "",
        "The following master-letter glyphs were absent, encoded as `.notdef`, or represented by a 1x1 placeholder and were substituted from nearby historical fonts.",
        "",
        "| Font | Letter key | Hebrew value | Donor font | Donor key |",
        "| --- | --- | --- | --- | --- |",
    ]
    alphabet = load_alphabet()
    key_to_hebrew = alphabet.key_to_hebrew()
    for filename, char, source, source_char in substitutions:
        lines.append(f"| `{filename}` | `{char}` | {key_to_hebrew[char]} | `{source}` | `{source_char}` |")
    if not substitutions:
        lines.append("| none in this repair pass | | | | |")
    lines.extend(
        [
            "",
            "The following glyphs existed but were judged historically or visually wrong for their assigned letter and were replaced deliberately.",
            "",
            "| Font | Letter key | Hebrew value | Donor font | Donor key |",
            "| --- | --- | --- | --- | --- |",
        ]
    )
    for filename, char, source, source_char in replacements:
        lines.append(f"| `{filename}` | `{char}` | {key_to_hebrew[char]} | `{source}` | `{source_char}` |")
    lines.extend(
        [
            "| `01-bc32c-egyptian-hieroglyphs-noto-sans.ttf` | `c` `q` `r` `s` `t` | צ ק ר ש ת | original Noto Egyptian signs | corrected ancestry order after deleting the extra Pe candidate |",
            "",
            "Several missing or weak ayin forms were replaced from local Proto-Sinaitic SVG drawings or the Tel Dan ayin, as listed above.",
            "",
            "The Serabit el-Khadim and Wadi el-Hol inscription fonts were also emboldened after substitution so their row weight matches the other Proto-Sinaitic comparison fonts more closely.",
            "",
        ]
    )
    README.write_text("\n".join(lines), encoding="utf-8")


def main() -> int:
    alphabet = load_alphabet()
    key_to_hebrew = alphabet.key_to_hebrew()
    master_for_hebrew = {alphabet.HEBREW_BY_NAME[name]: key for key, name in alphabet.MASTER_ASCII.items()}

    rename_files()
    preserve_serabit_light()
    filenames = [path.name for path in font_files()]
    fonts = {path.name: fontforge.open(str(path)) for path in font_files()}
    original_versions = {name: getattr(font, "version", "") for name, font in fonts.items()}
    for font in fonts.values():
        font.encoding = "UnicodeFull"
    substitutions: list[tuple[str, str, str, str]] = []

    master_keys = list(alphabet.MASTER_ASCII.keys())

    for filename in filenames:
        font = fonts[filename]
        for char in master_keys:
            if has_real_glyph(font, char):
                continue
            if char in FINAL_FORM_FALLBACKS and has_real_glyph(font, FINAL_FORM_FALLBACKS[char]):
                source_name, source_char = filename, FINAL_FORM_FALLBACKS[char]
            else:
                source_name, source_char = SUBSTITUTION_OVERRIDES.get(
                    (filename, char),
                    nearest_valid_source(fonts, filenames, filename, char),
                )
            copy_glyph(fonts[source_name], source_char, font, char)
            substitutions.append((filename, char, source_name, source_char))

    if EGYPTIAN_FONT in fonts:
        repair_egyptian_tail(fonts[EGYPTIAN_FONT])

    replacements: list[tuple[str, str, str, str]] = []
    if "04-bc15c-proto-sinaitic-15th-century-e.ttf" in fonts:
        generic_upper = fonts["04-bc15c-proto-sinaitic-15th-century-e.ttf"]
        copy_glyph(generic_upper, "g", generic_upper, "d")
        copy_glyph(fonts["04-bc15c-proto-sinaitic-15th-century-b.ttf"], "g", generic_upper, "g")
        replacements.append(
            (
                "04-bc15c-proto-sinaitic-15th-century-e.ttf",
                "d",
                "04-bc15c-proto-sinaitic-15th-century-e.ttf",
                "g",
            )
        )
        replacements.append(
            (
                "04-bc15c-proto-sinaitic-15th-century-e.ttf",
                "g",
                "04-bc15c-proto-sinaitic-15th-century-b.ttf",
                "g",
            )
        )

    source_fonts: dict[str, object] = {}
    if WADI_FONT in fonts:
        source_name = "fonts/proto-sinaitic-15.ttf"
        source_fonts[source_name] = fontforge.open(str(ROOT / source_name))
        source_fonts[source_name].encoding = "UnicodeFull"
        copy_glyph(source_fonts[source_name], "s", fonts[WADI_FONT], "d")
        fit_glyph(fonts[WADI_FONT], "d")
        replacements.append((WADI_FONT, "d", source_name, "s"))
    if SERABIT_FONT in fonts:
        source_name = "fonts/proto-sinaitic-other.ttf"
        source_fonts[source_name] = fontforge.open(str(ROOT / source_name))
        source_fonts[source_name].encoding = "UnicodeFull"
        copy_glyph(source_fonts[source_name], "s", fonts[SERABIT_FONT], "d")
        fit_glyph(fonts[SERABIT_FONT], "d")
        replacements.append((SERABIT_FONT, "d", source_name, "s"))

    for (filename, char), (source_name, source_char) in GLYPH_REPLACEMENTS.items():
        if filename in fonts and source_name in fonts:
            copy_glyph(fonts[source_name], source_char, fonts[filename], char)
            replacements.append((filename, char, source_name, source_char))

    for (filename, char), (svg_path, scale_factor) in SVG_REPLACEMENTS.items():
        if filename in fonts:
            import_svg_to_key(fonts[filename], char, svg_path, scale_factor)
            replacements.append((filename, char, svg_path, "svg"))

    if WADI_FONT in fonts:
        fill_hollow_components(fonts[WADI_FONT])

    for (filename, char), (image_path, scale_factor) in BITMAP_REPLACEMENTS.items():
        if filename in fonts:
            import_bitmap_alpha_to_key(fonts[filename], char, image_path, scale_factor)
            replacements.append((filename, char, image_path, "bitmap alpha"))

    for filename in filenames:
        font = fonts[filename]
        for key, hebrew in key_to_hebrew.items():
            source_char = master_for_hebrew[hebrew]
            if key == source_char:
                continue
            copy_glyph(font, source_char, font, key)
        if filename in BOLDEN_FONTS and not str(original_versions.get(filename, "")).startswith("1."):
            embolden(font, 24)
        update_metadata(font, filename)
        prune_to_key_layer(font, {ord(key) for key in key_to_hebrew} | {ord(" "), 0, 13})

    for filename, font in fonts.items():
        font.generate(str(FONT_DIR / filename))
        font.close()
    for font in source_fonts.values():
        font.close()

    write_readme(substitutions, replacements)
    print(f"repaired {len(filenames)} fonts; {len(substitutions)} master substitutions")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
