#!/usr/bin/env bash

lord --verbose << "EOF"
Try to speed up the parallel build in 'make parallel' by equally distributing the build jobs -- one per book-chapter -- equally among cores and not just having one process per book, since ezra will always finish much quicker than genesis, and try to cache as much as possible so as to never redo work unless you need to. If I haven't changed any of the tex files for Ezra, it shouldn't need to build twice. It shouldn't even need to build once if we're willing to do without absolute page numbers, but let's save that task for later. For now, I want you to make the parallel build be one chapter per pdf, and equally distribute the pdf build processes across cores without breaking the current nice ncurses interface that puts the completed chapters for each book in a specific tui square for that book specifically. Keep that layout. Use multiprocessing queues and producer consumer model prolifically to separate the build jobs from the reporting of a job's completion to the process whose job is to update either the interface or a given square within it, and never do any build jobs unless you need to. Make the Makefile logic for the parallel build be as make-correct as a large C or C++ job. If it doesn't need to be done, don't do it. Work until you've got the system in place. BEFORE YOU DO ANYTHING HOWEVER, add two make targets: one called 'make uncomment-one' that comments out every chapter of every book of the bible except chapter 1 of each book, and the second called 'make uncomment-two' that comments out everything except the first and last chapter of every book. Then complete the task above, and if you ever need to test the code by building anything, use either 'make uncomment-one' or 'make uncomment-two', or else a similar 'one' and 'two' model that's appropriate to the parallel build if you think that approach is better. Tl;dr: You can deviate from my instructions in minor ways in order to get a much better result. But do everything above, or (if you deviate from it) something much better, and make the parallelism noticeably faster both by (1) being divided up properly so as to always saturate all cores when jobs are available to do so, as described above, and also (2) not rebuild anything you don't need to. Finally, don't make 'make parallel' do both passes by default. Make 'make parallel2' do that. But 'make parallel' should just do one pass. Make it good, and impress me with the elegance, simplicity, and efficiency of your solution.
EOF
