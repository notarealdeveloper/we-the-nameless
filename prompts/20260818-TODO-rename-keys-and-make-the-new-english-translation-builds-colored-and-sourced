#!/usr/bin/env bash

lord --verbose << "EOF"
In bin/book-map-translation, bin2/book-map-translation2, and in the translations/*/sources.json files, instead of the keys being 'hebrew' and 'text' and 'kjv' (or whatever the translation's name is) make the keys be 'hebrew' and 'english' and 'translation', and make sure the 'translation' key is the one that's used by the build system as the english text in the output pdf whenever we build the book with a config that specifies any non-default translation like kjv or nrsv. That is, I want the keys renamed in sources.json and the scripts that generate them, also rename the keys in the output of book-parse. I'm looking ahead to a future where the 'translation' may be either any english translation or possibly even a non-english translation, so the hebrew and english keys are what they are, but the translation key is the output that should be placed in the final pdf, in the left of each verse block, with original hebrew on the right and commentary on the bottom. In other words, the english is my REFERENCE english that contains the source annotation work, since I speak english. But now I want the test the system's ability to make other translations (e.g., kjv and nrsv) also build a pdf with SOURCED and COLORED english output from THEIR particular english translations. Do that now, and don't stop until you're satisfied with the result. To clarify, after you finish, building the book with a non-default config should generate a SOURCED and COLORED english text of the kjv or nrsv in the english blocks of each verse in the final pdf. Do that now, until you're satisfied.
EOF
