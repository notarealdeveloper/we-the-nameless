#!/usr/bin/env bash

lord --verbose << "EOF"
Take the data in translations/kjv/ and format it like the data in translations/default/. Do the same for translations/nrsv/. Then write me a python script in bin/ called book-parse-markdown. It should take the path to any directory or file that is formatted like of one dirs in translations/ or any of its subdirs or the files inside it (i.e., I should be able to pass it book-parse-markdown ./translations/default/ or ./translations/nrsv/01-genesis/ or ./translations/kjv/01-genesis/22.md) and get back a json representation of all the data contained in the books, chapters, and verses recursively below that directory, with superscripts turned into ascii digits, and optionally with leading and trailing whitespace stripped if passed the -s/--strip flag. Then create book-parse-tex with the same flags, and make it parse the files in 01-genesis/*.tex and so on, accepting either a dirname or a tex file or a dir of subdirs of tex files. You're done when (1) translations/kjv/ and translations/nrsv/ are formatted like the hierarchy in ./translations/default, and (2) the book-parse-markdown script works on all three of those directories in ./translations, and when (3) the book-parse-tex script works for all of the dirs in [0-9]-*/ and all the files in [0-9]-*/*.tex. Don't modify any tex files. Just verify that the scripts parse things correctly. Then finally, write book-parse, a simple python script that dispatches to book-parse-markdown or book-parse-tex depending on a file's extension, returning the same data format for both tex and markdown files. Do that now, and keep working until the data's in place, and the scripts all work as expected.
EOF
