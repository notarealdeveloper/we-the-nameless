#!/usr/bin/env bash

lord --verbose << "EOF"
# PROMPT TO WRITE A PROMPT TO REFACTOR THE BOOK'S INFRASTRUCTURE FROM THE GROUND UP

Write me a long prompt that contains (1) Everything you just said, as background information, and (0) Before that, a request to reimplement everything that's currently in the file master.tex in the repo root into a new file mistress.tex that has *exactly the same semantics* (minor differences are ok if inevitable so as not to bloat the code) as master.tex, but which dramatically simplifies and reorganizes the implementation of everything in the book, by storing the majority of the data structures and config information in a set of Lua data structures which are managed with LaTeX macros that get and/or set the data in those data structures. Basically the goal is to make the resulting file as simple and elegant as possible, while preserving the choices that I appear to have put a lot of work and thought into. The goal should be to delete as much code as possible of the "tangled bloated latex" variety, while preserving the infrastructure of the book that I've built up over time. Also re-order the nav sections so that they flow in a principled way, from config at the top through packages, fonts for all the writing systems, down through types of authors (e.g., Aaronid, Mushite, etc), then the individual authors and which categories they belong to and which fonts they use for hebrew and english, which defines the authors' source profiles, and then in an ordered sequence from large details to small details of what a book fundamentally is, namely macros and code defining, in order: the title page layout and macros (but not content), the front matter layout and macros (but not content) of the table of contents which has one line per book, then the layout (but not content) of books (genesis, exodus, etc) including THEIR book title pages' layouts and book-specific ToCs layouts (but not content), then down from book level to chapter level formatting, including the chapter title pages etc, then down from chapters to pages, where source header definitions and page related macros should be, then down from page to the level of verse with all its macros, there is where we define the incredibly important \\Verse macro, then down from the level of Verses to the blocks within each verse, namely the Hebrew block, the English block, and the commentary block. Then the commentary authors' macros like \\aA{} \\aB{} and \\aC{} which should be, as they are now, default text color (switching from black-ish to white-ish when dark mode is toggled) and the red and blue respectively, down to the level of words, specifically the macros the permit us to make certain words configurable, which at present are words like \\God, and the redaction jokes like \cum and \pussy and so on which expand to things like \c\redacted{um}\eR{hildren} and so on, and so now we're at the level of individual words.

I don't think there's anything related to individual letters (except the font material which belongs much earlier, so we're good here) and then once we've got all that, we have ALL THE REUSABLE MACROS SPECIFYING LAYOUT AND FORMAT, BUT NONE OF THE CONTENT! So now we run through the book again in the same order, providing the content: First comes the book binding, which should contain the title page's *content* and feed that content to the title page macros defined far above this. Then we provide the CONTENT of the table of contents using its macros, then the content of the alphabet history pages (ideally simplified vs the current implementation, but don't change the spacing, it's very fine tuned, and then the source key pages content, defined using their macros. Then we come to the book proper. We should then see the Genesis book's book-specific table of contents CONTENT which is mostly just the chapter summaries fed to some simple macros, line after line, then you can cluster all the book-specific TOCs with their own chapter summaries together in one place. Then the long list of \Book{} and the includes of each chapter of each book like we have now. So in summary, write a long prompt starting with (0) the request I just gave above, and then (1) everything you just said, as advice to codex on how to actually carry out the implementation such that the result ends up dramatically simpler than it currently is.
EOF
