Attached is the output of a earlier local codex prompt in which I said "Run a word count on the English \eJ{}, \eE{}, and \eP{} sections of the whole Torah, and give me a frequency analysis in 3 json files of the most common words in each. Give the full list of all words used by each, no need to abbreviate."

I don't want you to use the work of the previous attempt. I'm just attaching the outputs to give you an idea of the direction of our previous research in this direction.

What I want you to do is to produce a full machine readable dataset and a python library for consuming, plotting, and displaying the information from that data set.

Here's what I've attached:

- A set of .tex files, in primary-history.tar.gz, containing paired verse by verse Hebrew together with the Friedman translation of genesis through deuteronomy, and a decent but less satisfactory english translation of the chapters after that. Those .tex files contain, importantly, a source assignment: an attribution to J, E, P, D (Dtr1, Dtr2, and Dtn for the law code), R (the redactor of the torah), RJE (the redactor of J and E), Other (e.g., Gen 14's anomalous source), and various Proto sources for ancient Yahwistic poetry.

- The outputs of the experiments mentioned previously, which show, among other things, the characteristic language of J, E, and P, not only in the well known parts of vocabulary, but also in the long tail of less commonly discussed evidence that shows up inevitably in any full frequency diagram of all word usage by a given author.

Here's what the dataset and python library that I want you to produce should be able to do. You can achieve this behavior however you want, and you're free to, but not required to, use the attached files to provide partial solutions to some of the problems.

1. The library should have functions that can accept any english word, phrase, or regex, and give a list of Verse objects that match that word, phrase, or regex, with source information included, extracted from the .tex files. The .tex files' source assignment is the ground truth. You can assume that the library will be run in a directory or given the path to a directory via some env var that contains at least the data in the attached `tree` command output. A Verse object should know its hebrew text, english text, book, chapter, verse numbers, and the source assignments to the full string or to substrings if multiple sources. The Chapter and Book objects should behave similarly, give each a good repr. Book's repr should be light grey, Chapter red, Verse blue.
2. Same as 1, but for hebrew. It should accept Hebrew strings or regexes, and should ignore spaces in the Heb unless the user passes a flag saying spaces=True. This is because the Heb spaces are less solidly knowable than the eng, and also because I believe biblical authors often did wordplay across word boundaries, or else translators got an occasional word boundary wrong.
3. By default, the Hebrew functions should ignore niqqud, but all relevant functions should accept niqqud=True, and there should be a module level setter and getter for changing this globally.
4. There should be functions for performing frequency analysis on any subset of books. By default, the Primary History: Genesis through 2 Kings. Other books may be available in some subdirs, but don't assume they are. Just assume they're in this tex format.
5. Separate the tex parsing from the functions that operate on english or hebrew strings in isolation. Expose both. The library should be usable with a bit of user led parsing on stuff other than just the tex files of this format.
6. Back to the frequency analysis. The functions should accept an english word, or a heb word with or without niqqud, optionally ignoring internal matres or all matres, and should return a list of Verse objects that match that word. English phrases should not care about capitalization by default but should care about spaced. Heb phrases should not care about spaces or niqqud by default. The greps that return a list, and the frequency analysis that turns a `class Verses(list):` object into a subclass of `collections.Counter` should work even if I cross word boundaries in the Hebrew. e.g., באש in the burning bush scene does not show up in normal concordances, because it's parsed as ב + אש, but if J is making a pun on the Heb. terms באש and בער there meaning "smelly" and "stupid" respectively, I should be able to find באש by searching for it.
7. However, for purposes of Hebrew frequency analysis, you can use the spaces in the provided files if you need to.
8. The library should be able to give a full frequency analysis by source of any eng or heb word. See words.txt and the other words* files for the sort of thing I mean.
9. Words by default do not end at ' or - or other unicode oddities, words end only at spaces. The concept of `class Word(str)` is most important for the frequency analysis, not for the arbitrary greps that the library provides, returning a `class Verses(list)`.
10. Finally, given any novel string in biblical Hebrew that the library has never seen before, it should be able to use the frequency analysis of the torah or of other parts of the primary history to give a quantifiable metric of evidence for that sentence having been written by any of the sources for which the library typically performs frequency analysis, especially J, E, P, R, and D (the combined Dtr1, Dtr2, Dtn, and Deuteronomistic Historian source you'll find in the tex files). If the second half of Joshua was written by P, we should be able to provide evidence for that by using a frequency analysis on the torah + the evidence functions in the repo.
11. Importantly, the library should not only use common source author specific words like "Tabernacle" and "congregation" and "gold" for P and so on. It should be able to use the "long tail" of word usage from all the words across all the sources, primarily in the Torah where our source knowledge is stronger, but in principle from other sections too. For source attribution, the repo's functions should by default base its conclusions on the evidence of word frequency by author in the Torah alone. We trust the assignment in the Torah. Anything else is speculative, and should be supported by the evidence we get from the library.
12. Add functions for making plots, add tests, add a pyproject.toml, and a Makefile. See the attached python repo `embd.tar.gz` for the layout that I want you to give to the repo. Give it both a cli interface with argparse, and a python importable interface.

Work on everything above until you're satisfied with the result, then link me the repo. And whenever my requests above seem contradictory or unclear, do your best to make the simplest, best, and most elegant decision of how to carry out the implementation. For example, if the repo wants to cache the results of large computations like frequency analysis in ~/.cache/repo_name/somthing by default, great! Just add function for cleaning the cache and then it's fine to cache by default and use existing caches by default. Finally, name the repo: `yhwh`

Work on everything above until you're satisfied with the result, then link me the repo.